apiVersion: v1
kind: Service
metadata:
  name: renovate-alpine-datasource
spec:
  type: ClusterIP
  selector:
    app: renovate-alpine-datasource
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
